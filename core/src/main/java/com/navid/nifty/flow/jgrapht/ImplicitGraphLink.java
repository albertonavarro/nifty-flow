package com.navid.nifty.flow.jgrapht;

/**
 * This module represents implicit links between screens of the same module (like PREV and NEXT)
 */
public class ImplicitGraphLink extends GraphLink {
    public ImplicitGraphLink(String linkName) {
        super(linkName);
    }
}
